require 'spec_helper'

describe Sinatra::Pundit do
  it 'has a version number' do
    expect(Sinatra::Pundit::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
